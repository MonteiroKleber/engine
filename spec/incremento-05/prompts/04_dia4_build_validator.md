# Prompt — Dia 4: Build Validator (repo gerado)

Implemente o Dia 4 da Semana 7.

Criar `/home/bazari/engine/validators/build_validator.py`.

Execução de comandos em:
- `/home/bazari/generated/<project>/`

Comandos mínimos:
- Backend (em `backend/`): `mvn test`
- Frontend (em `frontend/`): `npm ci` e `npm run build`

Critério de aceite:
- Repo vazio (templates puros) passa no build.
