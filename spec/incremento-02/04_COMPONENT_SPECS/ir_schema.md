# schemas/ir.schema.json — Especificação

## Objetivo
Adicionar o schema oficial do IR canônico em `schemas/ir.schema.json`.

## Fonte do schema
- Usar **exatamente** o schema definido na Semana 2 (IR com `meta`, `domain`, `api_intent`, `ui`, `nfr`).
- Não alterar nomes de campos nem requisitos.

## Critério de aceite (Dia 1)
- `tests/test_ir_schema.py` consegue carregar `schemas/ir.schema.json` sem erro.
