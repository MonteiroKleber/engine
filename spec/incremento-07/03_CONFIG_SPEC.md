# Configuração

## Semana 9
Nenhum campo novo obrigatório foi especificado.

## Premissas
- `blueprint_classifier` pode continuar retornando generic, mas a decisão final deve passar pelo `registry` com fallback FORCED_GENERIC.
- Run log deve sempre conter `blueprint`.
