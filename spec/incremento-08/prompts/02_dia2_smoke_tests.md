# Prompt — Dia 2: Smoke Tests (backend + frontend)

Implemente o Dia 2 da Semana 10.

Backend (repo gerado):
- Healthcheck: `GET /actuator/health` 200
- CRUD básico: `GET /api/<entityPlural>` 200/401 conforme RBAC

Frontend:
- `npm run build` ok
- Rota principal renderiza

Artefatos:
- `/home/bazari/generated/<project>/smoke/`

Aceite:
- Smoke executa localmente e retorna PASS/FAIL.
