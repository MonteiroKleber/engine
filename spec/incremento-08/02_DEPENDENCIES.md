# Pré-requisitos e ambiente

## Requisitos do sistema (execução local)
- `docker` e `docker compose`
- Backend: `mvn`
- Frontend: `npm`

## Semana 10
- Não adicionar dependências Python novas.
- Reusar build validator e patch engine existentes.
