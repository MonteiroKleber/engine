# Docker Compose FINAL (obrigatório)

## Objetivo
Garantir que o repo gerado tenha `docker-compose.yml` capaz de subir:
- postgres
- backend
- frontend

## Local
- `/home/bazari/generated/<project>/docker-compose.yml`

## Aceite (Dia 3)
- `docker compose up -d` sobe os 3 serviços sem erro.
