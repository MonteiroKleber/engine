# Saída FINAL (bundle industrial)

## Objetivo
Produzir um pacote de release com:
- Release Report (JSON/MD)
- caminho do repo gerado
- comandos:
  - subir: `docker compose up -d`
  - parar: `docker compose down`
- resumo do sistema

## Demo final
`python main.py --project demo --input "..." --release`

## Aceite (Dia 7)
- Sistema rodando.
- Smoke PASS.
- Logs completos.
