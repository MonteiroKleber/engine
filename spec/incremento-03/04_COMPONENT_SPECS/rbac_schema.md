# schemas/rbac.schema.json — Especificação

## Objetivo
Adicionar o schema oficial de RBAC em `schemas/rbac.schema.json`.

## Fonte do schema
- Usar **exatamente** o schema RBAC definido na Semana 2.
- Conteúdo esperado em alto nível: `roles` + `permissions` com `operation_id` + `required_role`.

## Critério de aceite (Dia 1)
- Schema RBAC carregável (por testes) sem erro.
