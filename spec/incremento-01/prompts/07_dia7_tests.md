# Prompt — Dia 7: Testes obrigatórios

Implemente:
- `tests/test_srs_schema.py`: carrega schema e valida um SRS gerado
- `tests/test_intake_flow.py`: roda engine (ou CLI) com input simples e confirma que:
  - gerou SRS
  - versionou no store
  - criou log do run

Aceite:
- `pytest` verde.
