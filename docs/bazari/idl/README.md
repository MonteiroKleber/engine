# Bazari — IDL/IR

- `bazari-mvp.idl`: IDL “completo” (inclui workflow/approval/transition; pode exigir expansão do dispatcher).
- `bazari-phase1.idl`: IDL “fase 1” (apenas CRUD/list via read; executável após Expansão 01).
- `bazari-mvp-ir.json`: IRCS v1 exportado a partir do DSL (gerado via console intake ou `parse_dsl`).

