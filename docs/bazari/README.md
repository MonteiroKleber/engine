# Bazari — Working Docs

Este diretório contém artefatos de trabalho da instituição Bazari (IDL/IR e docs).

IMPORTANTE: estes arquivos devem ficar **versionados** (no git) para evitar perda por limpeza de untracked
(ex.: `git clean`).

